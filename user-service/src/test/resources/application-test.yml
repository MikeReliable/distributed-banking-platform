spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver

  jpa:
    hibernate:
      ddl-auto: validate

  liquibase:
    enabled: true

  task:
    scheduling:
      enabled: false
